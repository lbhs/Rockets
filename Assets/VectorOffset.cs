using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class VectorOffset : MonoBehaviour
{
    public Vector2 BondingOffsetVector;  //This script is attached to InternalBondingTrigger GameObjects.  Each trigger needs to be assigned a Vector2D defining where the placement of the bond in x and y coordinates

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
